<?xml version="1.0" encoding="utf-8"?>
<!--
  This layout defines the appearance of a single row in our pizza list.
  It uses a RelativeLayout to provide flexibility in positioning the elements.
-->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp"
    android:background="#FFFFFF">

    <ImageView
        android:id="@+id/imgPizza"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_alignParentStart="true"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:scaleType="centerCrop"
        android:contentDescription="@string/pizza_image_description" />

    <TextView
        android:id="@+id/tvNom"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/imgPizza"
        android:layout_marginStart="12dp"
        android:layout_alignTop="@id/imgPizza"
        android:textSize="16sp"
        android:textStyle="bold"
        android:textColor="#333333" />

    <LinearLayout
        android:id="@+id/metaRow"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/imgPizza"
        android:layout_below="@id/tvNom"
        android:layout_marginStart="12dp"
        android:layout_marginTop="8dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tvMeta"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textSize="14sp"
            android:textColor="#777777" />

    </LinearLayout>

    <TextView
        android:id="@+id/chevron"
        android:layout_width="28dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="12dp"
        android:text="›"
        android:textSize="24sp"
        android:textColor="#AAAAAA"
        android:gravity="center" />

</RelativeLayout>